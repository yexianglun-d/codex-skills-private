interface:
  display_name: "Project Memory Manager"
  short_description: "Maintain a lightweight project journal"
  default_prompt: "Use $project-memory-manager to read or update docs/project-memory as a lightweight project journal: current status, activity log, decisions, and next handoff."

policy:
  allow_implicit_invocation: false
