interface:
  display_name: "AI Design Router"
  short_description: "Route UI and design tasks to the right skills"
  default_prompt: "Use $ai-design-router to choose the smallest useful set of design, frontend, Figma, motion, or visual QA skills for the current task."
policy:
  allow_implicit_invocation: true
