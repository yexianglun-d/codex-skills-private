interface:
  display_name: "Codex Project Orchestrator"
  short_description: "Advanced PRD and multi-thread project orchestration"
  default_prompt: "Use $codex-project-orchestrator only for PRD decomposition, milestones, task boards, multi-thread coordination, progress audits, or integration planning. For simple project history, use $project-memory-manager."
policy:
  allow_implicit_invocation: false
