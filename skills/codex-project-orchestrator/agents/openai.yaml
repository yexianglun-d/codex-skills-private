interface:
  display_name: "Codex Project Orchestrator"
  short_description: "Plan Codex multi-thread project execution"
  default_prompt: "Use $codex-project-orchestrator to initialize project control docs from my PRD and create milestones, tasks, and thread handoffs."
policy:
  allow_implicit_invocation: false
